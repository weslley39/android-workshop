<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColorHint">@color/textHint</item>
        <item name="colorControlActivated">@color/text</item>
        <item name="colorControlHighlight">@color/text</item>
        <item name="colorControlNormal">@color/textHint</item>

    </style>

    <style name="AppPrimaryButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:layout_width">150dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="backgroundTint">@color/primaryButton</item>
        <item name="android:textSize">@dimen/primary_button_text_size</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppForgotButton" parent="Base.Widget.AppCompat.Button">
        <item name="background">@android:color/transparent</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="android:textSize">@dimen/primary_button_forgot_text_size</item>
        <item name="android:textColor">@color/primaryButton</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/tabSelectedIndicator</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/AppTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/tabBarSelectedTitle</item>
        <item name="android:background">@color/tabBar</item>
    </style>

    <style name="AppTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/tab_layout_text_size</item>
        <item name="android:textColor">@color/tabBarTitle</item>
        <item name="textAllCaps">true</item>
    </style>

</resources>
